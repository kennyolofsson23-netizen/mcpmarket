// F014: Featured Listings
describe("GET /api/servers/featured", () => {
  it.todo("returns currently featured servers");
});
describe("POST /api/billing/featured", () => {
  it.todo("creates featured listing Stripe session");
  it.todo("rejects non-developer with 403");
});
