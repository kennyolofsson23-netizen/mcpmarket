// F012: Admin Dashboard
describe("GET /api/admin/stats", () => {
  it.todo("returns platform stats for admin");
  it.todo("rejects non-admin with 403");
});
describe("GET /api/admin/servers/pending", () => {
  it.todo("returns pending servers for admin");
});
describe("POST /api/admin/servers/:id/approve", () => {
  it.todo("approves server and notifies developer");
});
describe("POST /api/admin/servers/:id/reject", () => {
  it.todo("rejects server with reason");
});
describe("GET /api/admin/users", () => {
  it.todo("returns all users for admin");
});
