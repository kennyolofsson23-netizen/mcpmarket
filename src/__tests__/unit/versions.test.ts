// F018: Server Version Management
describe("POST /api/servers/:slug/versions", () => {
  it.todo("creates new version for server owner");
  it.todo("sets new version as latest");
  it.todo("rejects non-owner with 403");
});
describe("GET /api/servers/:slug/versions", () => {
  it.todo("returns version history for server");
});
